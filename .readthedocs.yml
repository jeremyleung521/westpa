# .readthedocs.yml

version: 2

build:
  image: latest

sphinx:
  configuration: doc/conf.py

python:
  version: 3.6
  install:
    - method: setuptools
      path: .
conda:
  environment: doc/doc_env.yaml
