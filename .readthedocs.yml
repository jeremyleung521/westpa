# .readthedocs.yml

version: 2

build:
  image: latest

sphinx:
  configuration: doc/conf.py

python:
  version: 3.6
  install:
    - method: pip
      path: .
    - method: setuptools
      path: setup.py
conda:
  environment: doc/doc_env.yaml
