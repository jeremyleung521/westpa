# .readthedocs.yml

version: 2

build:
  image: latest

sphinx:
  configuration: doc/conf.py

python:
  version: 3.7
  install:
    - method: pip
      path: -e .
conda:
  environment: doc/doc_env.yaml
