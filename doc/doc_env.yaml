name: documentation
channels:
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip

  # Dependencies
  - numpy
  - h5py
  - scipy
  - blessings

    # Pip-only installs
  #- pip:
  #  - codecov
